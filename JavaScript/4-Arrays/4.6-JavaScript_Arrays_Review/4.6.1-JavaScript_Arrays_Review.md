# By the end of this module, you should know:

## Basics
- Array definition
- Acessing elements from arrays
- `length` property
- update elements in an array 

## Two dimensional arrays
- Definition of two dimensional arrays

## Array destructuring 
- Definition of array destructuring
- Syntax to do it

## Common array methods
- `push()`
- `pop()`
- `shift()`
- `unshift()`
- `indexOf()`
- `lastIndexOf()`
- `splice()`
- `includes()`
- `concat()`
- `slice()`
- `[...]`
- `split()`
- `reverse()`
- `join()`
